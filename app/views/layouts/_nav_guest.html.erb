<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <span class="navbar-brand" href="/" style="margin-top: -11%"><a href='/'><%= image_tag("logo.png", class:'' , height:'40px') %></a></span>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <div class="btn-group nav navbar-form navbar-right">
        <button type="button" class="btn dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <span class="glyphicon glyphicon-info-sign"></span></button>
        <ul class="dropdown-menu">
          <li><a href="#">Forgot Password</a></li>
          <li><a href="#">Help</a></li>
        </ul>
      </div>
      <%= form_for(:session, :url => {:controller => 'sessions', :action => 'create'}, :html => {:class => 'nav navbar-form navbar-right'}) do |f| %>
        <div class="form-group">
          <%= f.text_field :u_id, :class => 'form-control' , :placeholder => 'Username' %>
          <%= f.password_field :password, :class => 'form-control' , :placeholder => 'Password' %>
        </div>
        
        <button type="submit" class="btn btn-default"><span class="glyphicon glyphicon-hand-right" aria-hidden="true"></span></button>
      <% end %>
    </div><!-- /.navbar-collapse -->
    <div style="float:right; margin-right:25%; margin-top: -0.8%;">
      <font color ="red"><%= flash[:danger] %></font>
    </div>
  </div><!-- /.container-fluid -->
</nav>